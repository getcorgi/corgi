import { Box } from '@material-ui/core';
import { styled } from '@material-ui/core/styles';

export const Streams = styled(Box)({
  backgroundColor: '#1e1f23b8',
  height: '100%',
  overflow: 'auto',
  position: 'relative',
});
