import { styled } from '@material-ui/core/styles';

export const Controls = styled('div')({
  alignItems: 'center',
  background: 'white',
  bottom: 0,
  display: 'flex',
  height: '72px',
  justifyContent: 'center',
  width: '100%',
});
