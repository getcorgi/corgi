import { Box, styled } from '@material-ui/core';

export const GameBar = styled(Box)({
  height: 300,
  background: '#17181b',
  justifyContent: 'space-between',
  display: 'flex',
  alignItems: 'center',
});
